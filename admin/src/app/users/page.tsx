'use client';

export default function UsersPage() {
  return (
    <div style={{ padding: 24 }}>
      <h1>Users</h1>
      <p style={{ color: '#666' }}>Admin API for listing users required (e.g. GET /admin/users with JWT).</p>
    </div>
  );
}
